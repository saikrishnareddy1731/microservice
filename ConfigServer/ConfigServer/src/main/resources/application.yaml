server:
  port: 9296


spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/saikrishnareddy1731/spring-app-config
          clone-on-start: true